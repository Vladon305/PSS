let initialState = [{
  id: 1,
  newPostTitle: 'Add title',
  newPostCategory: 'Add category',
  newPostText: 'Add text'
}];

const newValueReducer = (state = initialState, action) => {
  return state;
}

export default newValueReducer 